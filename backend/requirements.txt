fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
pydantic[email]==2.9.2
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
python-dotenv==1.0.1
psycopg2-binary==2.9.9
pytest==8.3.3
httpx==0.27.2

# Optional — only needed if you set ANTHROPIC_API_KEY to actually use
# app/routers/lab_reports.py's /extract endpoint. Left uninstalled by
# default so the rest of the app has no dependency on it; `pip install
# anthropic` before deploying with lab-report extraction turned on.
# anthropic==0.40.0
